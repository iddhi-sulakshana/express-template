---
description: HTTP Status codes usage
globs: ['src/core/status-codes.constants.ts']
alwaysApply: false
---

- Always use `HTTP_STATUS` constants.
- ❌ Do not hardcode numeric codes (e.g., 404, 500).
- ✅ Example:
  ```ts
  throw new ApiError('Not found', HTTP_STATUS.NOT_FOUND);
  ```
